# =============================================================================
# Qolsys Gateway Configuration Example
# =============================================================================
# Copy this file to .env and fill in your values
# Environment variables take precedence over YAML config file
#
# Required fields are marked with (required)
# Optional fields show their default values
# =============================================================================

# =============================================================================
# QOLSYS PANEL CONFIGURATION
# =============================================================================

# Panel hostname or IP address (required)
# The IP address or hostname of your Qolsys IQ Panel 2/2+/4
QOLSYS_PANEL_HOST=192.168.1.100

# Panel port (optional, default: auto-detected based on SSL connection)
# Usually 12345 or 12346 - leave unset for automatic detection
# QOLSYS_PANEL_PORT=12345

# Panel MAC address (optional, auto-detected if not set)
# Used to link device to Home Assistant network integration
# Format: AA:BB:CC:DD:EE:FF
# QOLSYS_PANEL_MAC=

# Panel secure token from Control4 interface (required)
# How to obtain:
#   1. On panel: Settings > Advanced Settings > Installation > Devices > Control4
#   2. Enable "Control4 Installed" if not already enabled
#   3. Note the "Secure Token" displayed on screen
QOLSYS_PANEL_TOKEN=your_panel_token_here

# User code for arming/disarming (optional, but recommended)
# Supports both 4-digit and 6-digit codes (depending on panel configuration)
# If not provided:
#   - Panel will still accept commands
#   - Some operations may require code entry on physical panel
#   - HA integration will require code for disarm operations
# QOLSYS_PANEL_USER_CODE=123456

# Unique identifier for this panel (optional, default: qolsys_panel)
# Used in MQTT topics and Home Assistant device identifiers
# Useful if running multiple panels in the same HA instance
# Must be unique across all panels, lowercase, alphanumeric with underscores
# QOLSYS_PANEL_UNIQUE_ID=qolsys_panel

# Device name for Home Assistant (optional, default: Qolsys Panel)
# The friendly name shown in Home Assistant device registry
# QOLSYS_PANEL_DEVICE_NAME=Qolsys Panel

# =============================================================================
# ARMING CONFIGURATION
# =============================================================================
# Control arm delays and bypass behavior

# Exit delay for arm away (optional, seconds)
# Override panel's default exit delay when arming away
# QOLSYS_ARM_AWAY_EXIT_DELAY=30

# Exit delay for arm stay (optional, seconds)
# Override panel's default exit delay when arming stay
# Common values: 0 (instant arm), 30, 60
# QOLSYS_ARM_STAY_EXIT_DELAY=0

# Allow bypass when arming away (optional, true/false)
# If true, bypassed sensors are automatically included in away mode
# QOLSYS_ARM_AWAY_BYPASS=false

# Allow bypass when arming stay (optional, true/false)
# If true, bypassed sensors are automatically included in stay mode
# QOLSYS_ARM_STAY_BYPASS=false

# Arm type for custom bypass (optional, default: arm_away)
# Which arm mode to use when custom bypass is requested
# Valid values: arm_away, arm_stay
# QOLSYS_ARM_TYPE_CUSTOM_BYPASS=arm_away

# =============================================================================
# MQTT BROKER CONFIGURATION
# =============================================================================

# MQTT broker hostname or IP address (required)
# The hostname or IP of your MQTT broker (e.g., Mosquitto)
# Common values: localhost, mosquitto, homeassistant.local
MQTT_HOST=localhost

# MQTT broker port (optional, default: 1883)
# Standard ports: 1883 (unencrypted), 8883 (TLS)
MQTT_PORT=1883

# MQTT authentication (optional)
# Leave blank if your broker doesn't require authentication
MQTT_USERNAME=
MQTT_PASSWORD=

# MQTT QoS level (optional, default: 1)
# Quality of Service level for MQTT messages
#   0 = At most once (fire and forget)
#   1 = At least once (default, recommended)
#   2 = Exactly once (highest overhead)
MQTT_QOS=1

# MQTT retain flag (optional, default: true)
# If true, MQTT messages are retained on the broker
# Recommended for state messages so they persist across restarts
# MQTT_RETAIN=true

# =============================================================================
# HOME ASSISTANT CONFIGURATION
# =============================================================================

# Home Assistant MQTT discovery prefix (optional, default: homeassistant)
# The prefix for Home Assistant MQTT discovery topics
# Change only if you've customized HA's MQTT discovery prefix
HA_DISCOVERY_PREFIX=homeassistant

# Check user code in Home Assistant (optional, default: true)
# If true, HA validates user codes locally before sending to panel
# If false, codes are sent directly to panel for validation
# Set to false if not using panel user code
# HA_CHECK_USER_CODE=true

# User code for Home Assistant (optional)
# A different code that HA can validate locally (if different from panel code)
# Must be 4 or 6 digits
# Only used if HA_CHECK_USER_CODE=true and you want HA validation separate from panel
# HA_USER_CODE=

# Require code for arming (optional, default: false)
# If true, Home Assistant will require code entry to arm the system
# HA_CODE_ARM_REQUIRED=false

# Require code for disarming (optional, default: false)
# If true, Home Assistant will require code entry to disarm the system
# Automatically set to true if panel user code is not provided
# HA_CODE_DISARM_REQUIRED=false

# Require code for triggering (optional, default: false)
# If true, Home Assistant will require code entry to trigger alarm
# HA_CODE_TRIGGER_REQUIRED=false

# Home Assistant status topic (optional, default: {HA_DISCOVERY_PREFIX}/status)
# Topic where Home Assistant publishes its birth message when it restarts
# Gateway subscribes to this to detect HA restarts and republish all entity configs
# Leave unset to use default: homeassistant/status (or {discovery_prefix}/status)
# HA_STATUS_TOPIC=homeassistant/status

# Home Assistant online payload (optional, default: online)
# Payload that indicates Home Assistant is online and ready
# When this message is received, gateway republishes all discovery configs
# HA_STATUS_ONLINE_PAYLOAD=online

# =============================================================================
# SENSOR CONFIGURATION
# =============================================================================

# Default device class for unknown sensors (optional, default: safety)
# The Home Assistant device class to use when sensor type cannot be mapped
# Common values: safety, motion, door, window, smoke, gas, moisture
# QOLSYS_SENSOR_DEFAULT_DEVICE_CLASS=safety

# Enable static sensors by default (optional, default: false)
# If true, sensors that don't send updates (keypads, sirens) are enabled by default
# If false, these sensors are disabled by default in HA (can be manually enabled)
# QOLSYS_SENSOR_ENABLE_STATIC_BY_DEFAULT=false

# =============================================================================
# TRIGGER CONFIGURATION
# =============================================================================

# Default trigger command (optional)
# The type of alarm to trigger when alarm is activated from Home Assistant
# Valid values:
#   TRIGGER            - Standard alarm (recommended)
#   TRIGGER_AUXILIARY  - Auxiliary alarm
#   TRIGGER_FIRE       - Fire alarm
#   TRIGGER_POLICE     - Police alarm
# Leave unset to require manual trigger type selection in HA
# QOLSYS_TRIGGER_DEFAULT_COMMAND=TRIGGER

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level (optional, default: INFO)
# Controls verbosity of application logs
# Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
#   DEBUG   - Very verbose, includes all MQTT messages and state changes
#   INFO    - Normal operations, connections, and important events (recommended)
#   WARNING - Only warnings and errors
#   ERROR   - Only errors
#   CRITICAL- Only critical errors
LOG_LEVEL=INFO

# =============================================================================
# OPTIONAL: YAML CONFIGURATION FILE
# =============================================================================

# Path to optional YAML configuration file (optional)
# If provided, loads configuration from YAML file
# Environment variables take precedence over YAML values
# Useful for complex configurations or managing multiple environments
# CONFIG_FILE=/config/qolsysgw.yaml

# =============================================================================
# DOCKER-SPECIFIC NOTES
# =============================================================================
#
# When running in Docker, make sure to:
# 1. Mount this file or set environment variables in docker-compose.yaml
# 2. Use service names for network addresses (e.g., MQTT_HOST=mosquitto)
# 3. Ensure the gateway can reach both the panel and MQTT broker
# 4. Panel must be on the same network or routable from container
#
# Example docker-compose.yaml:
#   services:
#     qolsysgw:
#       image: ghcr.io/your-repo/qolsysgw:latest
#       env_file: .env
#       # Or use environment variables directly:
#       # environment:
#       #   - QOLSYS_PANEL_HOST=192.168.1.100
#       #   - MQTT_HOST=mosquitto
#       depends_on:
#         - mosquitto
#       restart: unless-stopped
#
# =============================================================================
