# Qolsys Gateway Configuration Example
# Copy this file to .env and fill in your values

# =============================================================================
# QOLSYS PANEL CONFIGURATION
# =============================================================================

# Panel hostname or IP address (required)
QOLSYS_PANEL_HOST=192.168.1.100

# Panel secure token from Control4 interface (required)
# Obtain from: Panel Settings > Advanced Settings > Installation > Devices > Control4 > Secure Token
QOLSYS_PANEL_TOKEN=your_panel_token_here

# User code for arming/disarming (optional, but recommended)
# Supports both 4-digit and 6-digit codes (depending on panel configuration)
# If not provided, panel may require code entry on physical panel
QOLSYS_PANEL_USER_CODE=123456

# Unique identifier for this panel (optional)
# Useful if running multiple panels - used in MQTT topics
# QOLSYS_PANEL_UNIQUE_ID=main_panel

# =============================================================================
# MQTT BROKER CONFIGURATION
# =============================================================================

# MQTT broker hostname or IP address (required)
MQTT_HOST=localhost

# MQTT broker port (default: 1883)
MQTT_PORT=1883

# MQTT authentication (optional)
MQTT_USERNAME=
MQTT_PASSWORD=

# MQTT birth topic - published when gateway connects (default: homeassistant/status)
MQTT_BIRTH_TOPIC=homeassistant/status

# MQTT birth payload (default: online)
MQTT_BIRTH_PAYLOAD=online

# MQTT will topic - published when gateway disconnects (default: homeassistant/status)
MQTT_WILL_TOPIC=homeassistant/status

# MQTT will payload (default: offline)
MQTT_WILL_PAYLOAD=offline

# MQTT QoS level (default: 1)
# 0 = At most once, 1 = At least once, 2 = Exactly once
MQTT_QOS=1

# =============================================================================
# HOME ASSISTANT CONFIGURATION
# =============================================================================

# Home Assistant MQTT discovery prefix (default: homeassistant)
HA_DISCOVERY_PREFIX=homeassistant

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# OPTIONAL: YAML CONFIGURATION FILE
# =============================================================================

# Path to optional YAML configuration file
# Environment variables take precedence over YAML config
# CONFIG_FILE=config.yaml